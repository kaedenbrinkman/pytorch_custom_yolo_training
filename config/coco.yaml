# train and val datasets (image directory or *.txt file with image paths)
train: data/artifacts/train.txt
val: data/artifacts/val.txt
test: data/artifacts/test.txt

# number of classes
nc: 1

# class names
names: ['Train']